# 🔒 Настройка безопасности

## ⚠️ КРИТИЧЕСКИ ВАЖНО

Перед запуском сервера необходимо настроить переменные окружения!

## 📝 Шаг 1: Создайте файл .env

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

## 📝 Шаг 2: Заполните .env

Откройте `.env` и заполните следующие переменные:

### JWT_SECRET (ОБЯЗАТЕЛЬНО!)

Сгенерируйте безопасный ключ:
```bash
openssl rand -base64 32
```

Или используйте онлайн генератор: https://www.lastpass.com/features/password-generator

**Пример:**
```env
JWT_SECRET=9QErZi0KfaChVnHQ4vrJNISfUfo0ySfUrNTbG1rSQDE=
```

### ALLOWED_ORIGINS

Укажите домены вашего приложения (через запятую, БЕЗ пробелов):

```env
ALLOWED_ORIGINS=https://my-chat-app.vercel.app,http://localhost:3000,http://localhost:8080
```

### DATABASE_URL

Укажите URL вашей базы данных (если еще не указан):

```env
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require
```

## 📝 Шаг 3: Миграция паролей

После настройки .env запустите миграцию паролей:

```bash
node migrate_passwords.js
```

Этот скрипт перехеширует все существующие пароли в базе данных.

**⚠️ ВАЖНО:** После успешной миграции удалите файл `migrate_passwords.js`!

## ✅ Проверка

После настройки проверьте:

1. ✅ Файл `.env` создан и заполнен
2. ✅ `JWT_SECRET` установлен (минимум 32 символа)
3. ✅ `ALLOWED_ORIGINS` содержит ваши домены
4. ✅ Миграция паролей выполнена
5. ✅ Сервер запускается без ошибок

## 🚀 Запуск сервера

```bash
npm start
```

---

**НЕ КОММИТЬТЕ .env В GIT!** Файл уже добавлен в .gitignore.

