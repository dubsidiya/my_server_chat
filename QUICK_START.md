# 🚀 Быстрый старт после обновления безопасности

## ✅ Шаг 1: Зависимости установлены

Зависимости уже установлены командой `npm install`.

## 📝 Шаг 2: Создайте файл .env

```bash
cd my_serve_chat_test
cp .env.example .env
```

## 🔑 Шаг 3: Настройте .env

Откройте `.env` и заполните:

### 1. JWT_SECRET (ОБЯЗАТЕЛЬНО!)

Сгенерированный ключ: `9QErZi0KfaChVnHQ4vrJNISfUfo0ySfUrNTbG1rSQDE=`

Или создайте свой:
```bash
openssl rand -base64 32
```

Добавьте в `.env`:
```env
JWT_SECRET=9QErZi0KfaChVnHQ4vrJNISfUfo0ySfUrNTbG1rSQDE=
```

### 2. ALLOWED_ORIGINS

```env
ALLOWED_ORIGINS=https://my-chat-app.vercel.app,http://localhost:3000
```

### 3. DATABASE_URL (если еще не настроен)

```env
DATABASE_URL=your-database-url-here
```

## 🔄 Шаг 4: Миграция паролей

```bash
npm run migrate-passwords
```

Или:
```bash
node migrate_passwords.js
```

## ✅ Шаг 5: Запуск сервера

```bash
npm start
```

---

## ⚠️ ВАЖНО

1. **НЕ коммитьте .env в Git** - файл уже в .gitignore
2. **Используйте разные JWT_SECRET для dev/prod**
3. **После миграции удалите migrate_passwords.js**

---

## 🧪 Проверка работы

1. Зарегистрируйте нового пользователя
2. Войдите в систему (должен вернуться токен)
3. Попробуйте получить список пользователей с токеном
4. Попробуйте без токена (должна быть ошибка 401)

---

Готово! 🎉

